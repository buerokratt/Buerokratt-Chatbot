declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'Empty conversations'"
  method: post
  accepts: json
  returns: json
  namespace: backoffice
  allowlist:
    body:
      - field: chatIds
        type: string
        description: "Body field 'ChatIds'"

mapChatIds:
  call: http.post
  args:
    url: "[#CHATBOT_DMAPPER_HBS]/map_chat_ids"
    headers:
      type: json
    body:
      chats: ${incoming.body.chatIds}
  result: convertedIds

validate_input:
  switch:
    - condition: ${convertedIds.response.body.length > 0}
      next: emptyMessages
  next: return_badInput

emptyMessages:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/chat/update_message_to_empty_content"
    body:
      chats: ${convertedIds.response.body}
  result: emptyMessages

emptyDenormalizedChatMessages:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/chat/update_denormalized_chat_message_to_empty_content"
    body:
      chats: ${convertedIds.response.body}
      updated: ${emptyMessages.response.body[0].updated}
  result: emptyDenormalizedMessages

return_result:
  return: ${emptyMessages.response.body}
  next: end

return_badInput:
  return: "Bad request."
  status: 400
  next: end
