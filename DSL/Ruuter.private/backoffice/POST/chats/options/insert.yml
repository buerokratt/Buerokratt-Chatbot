declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'OPTIONS-INSERT'"
  method: post
  accepts: json
  returns: json
  namespace: backoffice
  allowlist:
    body:
      - field: chatId
        type: string
        description: "Body field 'chatId'"
      - field: options
        type: string
        description: "Body field 'options'"

extractRequestData:
  assign:
    chatId: ${incoming.body.chatId}
    options: ${incoming.body.options}

get_title_visibility:
  call: http.get
  args:
    url: "[#CHATBOT_RESQL]/config/get_configuration"
    query:
      key: "is_csa_title_visible"
  result: title_res

getActiveChatById:
  call: http.get
  args:
    url: "[#CHATBOT_RESQL]/chat/get_chat_active_by_id"
    query:
      id: ${chatId}
      is_csa_title_visible: ${title_res.response.body[0]?.value}
  result: activeChatRes

checkActiveChatResult:
  switch:
    - condition: ${activeChatRes.response.body.length > 0}
      next: get_message_uuid
  next: return_chat_ended

get_message_uuid:
  call: http.post
  args:
    url: "[#CHATBOT_DMAPPER_HBS]/return_uuid"
    headers:
      type: json
  result: message_uuid_result

assignMessageUuid:
  assign:
    messageUuid: ${message_uuid_result.response.body.uuid}

add_message_to_db:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/chat/create_message_with_options"
    body:
      chatId: ${chatId}
      messageId: ${messageUuid}
      content: ""
      options: ${options ?? "[]"}
      authorRole: "buerokratt"
      rating: ""
      event: ""
      forwardedByUser: ""
      forwardedFromCsa: ""
      forwardedToCsa: ""
      authorId: ""
      authorFirstName: ""
      authorLastName: ""
  result: insertMessageResult

updateDenormalizedChatMessage:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/chat/update_denormalized_chat_message_fields_only"
    body:
      chatId: ${chatId}
      lastMessageTimestamp: ${insertMessageResult.response.body[0].updated}
      isSupportMessage: false
      isCustomerMessage: false
      authorTimestamp: ${insertMessageResult.response.body[0].updated}
      content: ""
      lastMessageAuthorId: ""
      lastMessageEvent: ""
  result: updateDenormalizedChatRes

updateDenormalizedChatMetricsMessageFields:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/chat/update_denormalized_chat_messages_for_metrics_message_fields_only"
    body:
      chatBaseId: ${chatId}
      messageId: ${insertMessageResult.response.body[0].id}
      messageBaseId: ${insertMessageResult.response.body[0].baseId}
      content: ""
      lastMessageTimestamp: ${insertMessageResult.response.body[0].updated}
      timestamp: ${insertMessageResult.response.body[0].updated}
      messageCreated: ${insertMessageResult.response.body[0].updated}
      messageUpdated: ${insertMessageResult.response.body[0].updated}
      messageEvent: ""
      messageAuthorRole: "buerokratt"
      messageAuthorId: ""
      messageForwardedFromCsa: ""
      messageForwardedToCsa: ""
  result: updateDenormalizedChatMetricsMessageRes

notify_chats:
  template: "[#CHATBOT_PROJECT_LAYER]/chats/notify"
  requestType: templates
  body:
    chatId: ${chatId}
  result: chats_notify_res
  next: notify_messages

notify_messages:
  template: "[#CHATBOT_PROJECT_LAYER]/messages/notify"
  requestType: templates
  body:
    chatId: ${chatId}
    type: "message"
    messageId: ${messageUuid}
  result: messages_notify_res

return_message_sent:
  status: 200
  return: "Message Sent Successfully"
  next: end

return_chat_ended:
  return: "Chat Has Ended"
  status: 400
  next: end
