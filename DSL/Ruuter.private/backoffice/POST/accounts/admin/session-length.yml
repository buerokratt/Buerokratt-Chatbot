declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'SESSION-LENGTH'"
  method: post
  accepts: json
  returns: json
  namespace: backoffice
  allowlist:
    body:
      - field: sessionLength
        type: string
        description: "Body field 'sessionLength'"
      - field: chatActiveDuration
        type: string
        description: "Body field 'chatActiveDuration'"
      - field: showIdleWarning
        type: string
        description: "Body field 'showIdleWarning'"
      - field: idleMessage
        type: string
        description: "Body field 'idleMessage'"
      - field: showAutoCloseText
        type: string
        description: "Body field 'showAutoCloseText'"
      - field: autoCloseText
        type: string
        description: "Body field 'autoCloseText'"
      - field: awayStatusActive
        type: string
        description: "Body field 'awayStatusActive'"
      - field: awayStatusTimeout
        type: string
        description: "Body field 'awayStatusTimeout'"

extractRequestData:
  assign:
    sessionLength: ${incoming.body.sessionLength}
    chatActiveDuration: ${incoming.body.chatActiveDuration}
    showIdleWarning: ${incoming.body.showIdleWarning}
    idleMessage: ${incoming.body.idleMessage}
    showAutoCloseText: ${incoming.body.showAutoCloseText}
    autoCloseText: ${incoming.body.autoCloseText}
    awayStatusActive: ${incoming.body.awayStatusActive}
    awayStatusTimeout: ${incoming.body.awayStatusTimeout}

setConfigurationValue:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/set-session-length-conf"
    body:
      created: ${new Date().toISOString()}
      session_length: ${sessionLength}
      chat_active_duration: ${chatActiveDuration}
      show_idle_warning: ${showIdleWarning}
      idle_message: ${idleMessage}
      show_auto_close_text: ${showAutoCloseText}
      auto_close_text: ${autoCloseText}
      away_status_active: ${awayStatusActive}
      away_status_timeout: ${awayStatusTimeout}
  result: res

return_result:
  return: ${res.response.body}
