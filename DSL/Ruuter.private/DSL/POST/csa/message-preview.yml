extractRequestData:
  assign:
    chatId: ${incoming.body.chatId}

getMessagePreview:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]/get-message-preview"
    body:
      chatId: ${chatId}
  result: res

returnSuccess:
  return: ${res.response.body}
