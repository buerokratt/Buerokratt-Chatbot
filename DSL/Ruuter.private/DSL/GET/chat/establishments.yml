getEstablishments:
  call: http.post
  args:
    url: "[#CHATBOT_RESQL]:[#CHATBOT_RESQL_PORT]/get-all-establishments"
    body:
      start: ${startDate}
      end: ${endDate}  
  result: res

return_result:
  return: ${res.response.body[0].names}
