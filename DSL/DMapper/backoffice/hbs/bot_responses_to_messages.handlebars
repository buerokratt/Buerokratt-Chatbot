[
{{#each data.botMessages}}
  {
   "chatId": "{{../data.chatId}}",
   "content": "{{#if (isType 'string' (choose text result))}}{{{filterControlCharacters (escapeQuotes (choose text result))}}}{{else}}{{choose text result}}{{/if}}",
   "buttons": "[{{#each buttons}}{\"title\": \"{{{title}}}\",\"payload\": \"{{{payload}}}\"}{{#unless @last}},{{/unless}}{{/each}}]",
   "authorTimestamp": "{{../data.authorTimestamp}}",
   "authorId": "{{../data.authorId}}",
   "authorFirstName": "{{../data.authorFirstName}}",
   "authorLastName": "{{../data.authorLastName}}",
   "created": "{{../data.created}}"
  }{{#unless @last}},{{/unless}}
{{/each}}
]
