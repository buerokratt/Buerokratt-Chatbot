call_the_service:
  call: http.post
  args:
    url: "[#SERVICE_RUUTER_PUBLIC]/services/active/${name}"
    dynamicParameters: true
    body:
      dynamicBody: ${incoming.body}
  result: service_call_result

return_ok:
  status: 200
  return: "Service called successfully" 
  next: end   
