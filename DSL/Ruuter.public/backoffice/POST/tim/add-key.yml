declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'ADD KEY'"
  method: post
  accepts: json
  returns: json
  namespace: backoffice
  allowlist:
    body:
      - field: chatId
        type: string
        description: "Body field 'chatId'"
      - field: timeout
        type: string
        description: "Body field 'timeout'"

extractRequestData:
  assign:
    chatId: ${incoming.body.chatId}
    timeout: ${incoming.body.timeout}
  next: return_success

return_success:
  status: 200
  return: "Key operation completed"
  next: end