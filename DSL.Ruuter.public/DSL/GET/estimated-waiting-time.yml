#returnByChatId:
#  return: returnSuccessWithChatId
#
#estimatedWaitingTime:
#  return: returnSuccess
#
#returnSuccessWithChatId:
#  return: "your chat id ${incoming.params.chatId}"
#  next: end

#assignFirst:
#  assign:
#    chatId: ${incoming.params.chatId}
#  next: returnSuccess

checkChatIdParameter:
  switch:
    - condition: ${!!incoming.params.chatId && incoming.params.chatId !== "" && incoming.params.chatId !== null}
      next: returnChatId
  next: returnSuccess

returnChatId:
  return: 100
  next: end

returnSuccess:
  return: "300"
  next: end

returnNotFound:
  return: "error: not found"
  next: end
